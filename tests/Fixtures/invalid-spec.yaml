openapi: 3.1.1
info:
  title: Invalid Test API
  version: 1.0.0
paths:
  /users:
    get:
      responses:
        '200':
          description: Success
  /users/{id}:
    get:
      # Missing required parameter definition for path parameter 'id'
      responses:
        '200':
          description: User found
  /posts/{postId}/comments/{commentId}:
    get:
      # Missing required parameter definitions for both path parameters
      responses:
        '200':
          description: Comment found